local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "The Chosen Hub",
    SubTitle = "v1.0.2",
    Author = "By Inky",
    Theme = "Sky"
})

local HomeTab = Window:Tab({ Title = "Dashboard", Icon = "house" })
local ScriptsTab = Window:Tab({ Title = "Scripts", Icon = "scroll" })
local AnimationsTab = Window:Tab({ Title = "Animations", Icon = "accessibility" })
local HubsTab = Window:Tab({ Title = "Hubs", Icon = "layers" })
local TrollingTab = Window:Tab({ Title = "Trolling", Icon = "flame" })
local GamesTab = Window:Tab({ Title = "Games", Icon = "gamepad-2" })

local HomeSection = HomeTab:Section({ 
    Title = "User Session Information",
    Opened = true 
})

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

HomeSection:Paragraph({
    Title = "Account Owner: " .. LocalPlayer.DisplayName,
    Desc = "Username: @" .. LocalPlayer.Name .. "\nAccount Age: " .. LocalPlayer.AccountAge .. " Days"
})

HomeSection:Button({
    Title = "Unload & Destruct UI",
    Callback = function()
        Window:Destroy()
    end
})

local ScriptsSection = ScriptsTab:Section({ 
    Title = "Universal",
    Opened = true 
})

ScriptsSection:Button({
    Title = "Infinite Yield",
    Desc = "One Of The Best.", 
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/edgeiy/infiniteyield/master/source"))()
            end)
        end)
    end
})

ScriptsSection:Button({
    Title = "Nameless Admin",
    Desc = "Admin Is Very Nameless",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/ltseverydayyou/Nameless-Admin/main/Source.lua"))()
            end)
        end)
    end
})

ScriptsSection:Button({
    Title = "Paranoia Admin",
    Desc = "An Admin Script",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://idx.lol/latest.lua"))()
            end)
        end)
    end
})

ScriptsSection:Button({
    Title = "Admin Panel",
    Desc = "Just A Regular Admin Script",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Admin-Panel-238222"))()
            end)
        end)
    end
})


ScriptsSection:Button({
    Title = "Youtube Music Player",
    Desc = "Launches the YouTube Music Player script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-YouTube-Music-Player-V8-236600"))()
            end)
            if not success then warn("Error loading YouTube Music Player: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Pshade (Shaders)",
    Desc = "Launches Pshade Ultimate shaders",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet('https://raw.githubusercontent.com/randomstring0/pshade-ultimate/refs/heads/main/src/cd.lua'))()
            end)
            if not success then warn("Error loading Pshade: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Shiftlock",
    Desc = "Launches Universal Shiftlock (persists after death)",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Universal-Shiftlock-No-Disappear-after-die-23318"))()
            end)
            if not success then warn("Error loading Shiftlock: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Fly",
    Desc = "Launches Fly Script Option 1",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://pastefy.app/9bilwWLP/raw"))()
            end)
            if not success then warn("Error loading Fly 1: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Fly 2",
    Desc = "Launches Fly Script Option 2",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Fly-script-v3-16742"))()
            end)
            if not success then warn("Error loading Fly 2: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Telekinesis Fling",
    Desc = "Launches Telekinesis Fling script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://pastebin.com/raw/biR1ZZA9"))()
            end)
            if not success then warn("Error loading Telekinesis Fling 1: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Telekinesis 2",
    Desc = "Lifts unanchored objects only",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Telekinesis-You-can-only-lift-unanc*d-things-237340"))()
            end)
            if not success then warn("Error loading Telekinesis 2: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Telekinesis 3",
    Desc = "Launches Telekinetic script variant",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-telekinetic-66554"))()
            end)
            if not success then warn("Error loading Telekinesis 3: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Telekinesis 4",
    Desc = "Launches Qwerty Telekinesis script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/randomstring0/Qwerty/refs/heads/main/qwerty1.lua"))()
            end)
            if not success then warn("Error loading Telekinesis 4: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Netless",
    Desc = "Launches Anti-hat fall netless script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Anti-hat-fall-82624"))()
            end)
            if not success then warn("Error loading Netless 1: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Delta Keyboard",
    Desc = "Launches Delta Keyboard bypass",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/Xxtan31/Ata/main/deltakeyboardcrack.txt", true))()
            end)
            if not success then warn("Error loading Delta Keyboard: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Verity V1",
    Desc = "Launches the Verity V1 script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://pastebin.com/raw/pQjRMdiG"))()
            end)
            if not success then warn("Error loading Verity V1: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Welding Abuse",
    Desc = "Launches Rexys Welding Hub",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/rangell8/Rexys-Welding-Hub/refs/heads/main/script"))()
            end)
            if not success then warn("Error loading Welding Abuse: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Fast Fredbear",
    Desc = "Launches the Fast Fredbear script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://pastebin.com/raw/wbEc0ThZ"))()
            end)
            if not success then warn("Error loading Fast Fredbear: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Wither Storm",
    Desc = "Launches the Wither Storm script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/ian49972/SCRIPTS/refs/heads/main/Wither"))()
            end)
            if not success then warn("Error loading Wither Storm: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Control Npcs",
    Desc = "Launches the NPC control script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/randomstring0/qwertys/refs/heads/main/qwerty8.lua"))()
            end)
            if not success then warn("Error loading Control Npcs: " .. tostring(err)) end
        end)
    end
})

ScriptsSection:Button({
    Title = "Coolkid Gui V1",
    Callback = function() 
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/cfsmi2/c00lguiv1/refs/heads/main/Main.lua"))()
            end)
            if not success then
                warn("Failed to load Coolkid V1: " .. tostring(err))
            end
        end)
    end 
})

ScriptsSection:Button({
    Title = "Coolkid Gui V2",
    Callback = function() 
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-c00lgui-134417"))()
            end)
            if not success then
                warn("Failed to load Coolkid V2: " .. tostring(err))
            end
        end)
    end 
})

ScriptsSection:Button({
    Title = "Coolkid Gui V2.5",
    Callback = function() 
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://gist.githubusercontent.com/wateringcancan-eng/209697b404ddee53bac61f2dca1237fc/raw/7458e1e9dfbc277a0fe508663b4e33972e93bb1e/C00lcl4n_2_0.lua"))()
            end)
            if not success then
                warn("Failed to load Coolkid V2.5: " .. tostring(err))
            end
        end)
    end 
})

ScriptsSection:Button({
    Title = "Coolkid Gui V3",
    Callback = function() 
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/MiRw3b/c00lgui-v3rx/main/c00lguiv3rx.lua"))()
            end)
            if not success then
                warn("Failed to load Coolkid V3: " .. tostring(err))
            end
        end)
    end 
})


local R15Section = AnimationsTab:Section({ 
    Title = "R15 Scripts",
    Opened = true 
})

R15Section:Button({ 
    Title = "R15 Emotes", 
    Callback = function() 
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/Bac0nHck/Scripts/refs/heads/main/BundleAnimations.lua"))()
            end)
        end)
    end 
})

R15Section:Button({ 
    Title = "Sonic R15", 
    Callback = function() 
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/Azizanzz0/FE-R15-Sonic-X-Super/refs/heads/main/Protected%20SonicXSuper.txt"))()
            end)
        end)
    end 
})

R15Section:Button({ 
    Title = "Drone R15", 
    Callback = function() 
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/randomstring0/qwertys/refs/heads/main/qwerty16.lua"))()
            end)
        end)
    end 
})

R15Section:Button({ 
    Title = "Ink Monster R15", 
    Callback = function() 
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/randomstring0/qwertys/refs/heads/main/qwerty14.lua"))()
            end)
        end)
    end 
})

R15Section:Button({ 
    Title = "R15 Mini Car", 
    Callback = function() 
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://pastefy.app/UqDEIOpO/raw"))()
            end)
        end)
    end 
})

R15Section:Button({ 
    Title = "R15 Katana", 
    Callback = function() 
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/randomstring0/qwertys/refs/heads/main/qwerty12.lua"))()
            end)
        end)
    end 
})

local R6Section = AnimationsTab:Section({ 
    Title = "R6 Scripts",
    Opened = true 
})

R6Section:Button({ 
    Title = "R6 Animation", 
    Callback = function() 
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://pastefy.app/UC2OHMhe/raw"))() 
            end)
        end)
    end 
})

R6Section:Button({ 
    Title = "Slasher Anim", 
    Callback = function() 
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-FE-Slasher-Delta-R6-201469"))()
            end)
        end)
    end 
})

local HatsSection = AnimationsTab:Section({ 
    Title = "Hats Or Reanimation" 
})

HatsSection:Button({
    Title = "Fe Noli Hats",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/randomstring0/qwertys/refs/heads/main/qwerty0.lua"))()
            end)
        end)
    end
})

HatsSection:Button({
    Title = "Fe Coolkid Hats",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/randomstring0/qwertys/refs/heads/main/qwerty2.lua"))()
            end)
        end)
    end
})

HatsSection:Button({
    Title = "Uhhh Reanimation",
    Callback = function()
        task.spawn(function()
            pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/STEVE-916-create/Uhhhhhh/main/source/reanim.lua"))()
            end)
        end)
    end
})

local HubsSection = HubsTab:Section({ 
    Title = "Hubs",
    Opened = true 
})

HubsSection:Button({
    Title = "Xvc Hub",
    Desc = "My Favorite.", 
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-XVC-Hub-202396"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "Altair Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://pastefy.app/hiwjpLFi/raw"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "Opiam Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/HydraRBXM/opiamhub/refs/heads/main/OpiamMain"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "Yunas Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/yunus154524/YunusLo1545-HUB/refs/heads/main/YunusLo1545%20HUB"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "DfxClan Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://pastebin.com/raw/HK1Xpajw"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "Galactic Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/GalacticScripts/Galactic-Hub/refs/heads/main/WindUI"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "Degent Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://pastebin.com/raw/cr20JxP9"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "Jarvis Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/HeyGyt/jarvisv1/refs/heads/main/jarvis"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "An Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet('https://raw.githubusercontent.com/GUI-Offical/FileTest/refs/heads/main/ANHUBB.lua'))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "Nyxmods Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-NyxMods-Universal-245609"))()
            end)
        end)
    end
})

HubsSection:Button({
    Title = "Ghost Hub",
    Callback = function()
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Ghost-hub-keyless-65732"))()
            end)
        end)
    end
})

TrollingTab:Button({
    Title = "Grab Npcs",
    Desc = "Launches Grab R6 NPC script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/GUI-Offical/FileTest/refs/heads/main/Grab%20R6.txt", true))()
            end)
            if not success then warn("Error loading Grab Npcs: " .. tostring(err)) end
        end)
    end
})

TrollingTab:Button({
    Title = "Dropkick Fling",
    Desc = "Launches DropKick fling script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/platinww/CrustyMain/refs/heads/main/universal/DropKick.lua"))()
            end)
            if not success then warn("Error loading Dropkick Fling: " .. tostring(err)) end
        end)
    end
})

TrollingTab:Button({
    Title = "Fling Gui",
    Desc = "Launches Universal Fling GUI",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/sypcerr/scripts/refs/heads/main/UFGUI", true))()
            end)
            if not success then warn("Error loading Fling Gui 1: " .. tostring(err)) end
        end)
    end
})

TrollingTab:Button({
    Title = "Spy Script",
    Desc = "Launches remote/chat spy tool",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(functionloadstring(game:HttpGet("https://gist.githubusercontent.com/DEVADMINENGINE/9bb547b158bc241a29a14bd43139a7aa/raw/7d7aff0bc1ff69be2b7e8bca848dfbb843399c91/dev%2520spyer"))()
            end)
            if not success then warn("Error loading Spy Script: " .. tostring(err)) end
        end)
    end
})

GamesTab:Button({
    Title = "Your Natural Disaster Hub",
    Desc = "Launches the Natural Disaster Survival hub script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/Inkyxen/Nds-Hub/refs/heads/main/Your%20Natural%20Disaster%20Hub"))()
            end)
            if not success then warn("Error loading Natural Disaster Hub: " .. tostring(err)) end
        end)
    end
})

GamesTab:Button({
    Title = "Your 99 Nights Hub",
    Desc = "Launches the 99 Nights hub script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/Inkyxen/Your-99-nights-hub/refs/heads/main/Your%2099%20Nights%20Hub"))()
            end)
            if not success then warn("Error loading 99 Nights Hub: " .. tostring(err)) end
        end)
    end
})

GamesTab:Button({
    Title = "Your Prison Life Hub",
    Desc = "Launches the Prison Life hub script",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/Inkyxen/Your-Prison-Life-Hub/refs/heads/main/Your%20Prison%20Life%20Hub"))()
            end)
            if not success then warn("Error loading Prison Life Hub: " .. tostring(err)) end
        end)
    end
})
